- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config:
      glob: true
      path: config/*
      relink: true
    ~/.bashrc_new:
      path: bashrc

- shell:
  - mv ~/.bashrc ~/.bashrc_old
  - mv ~/.bashrc_new ~/.bashrc
  - nvim +PlugInstall +qa
